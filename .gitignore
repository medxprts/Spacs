# Environment variables (contains sensitive API keys and credentials)
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs and databases
*.log
logs/
*.sqlite
*.db

# Temporary files
*.tmp
*.temp
.cache/

# PostgreSQL data (if running locally)
postgres_data/
pgdata/

# State files
.telegram_listener_state.json
orchestrator_state.json
agent_state.json
*.state

# Backup files
*.backup
*.backup_*
*.bak
backup-*/
backup_*.sql

# CSV data files (may contain sensitive data)
*.csv
data/*.csv

# Test outputs
test_outputs/
test_data/

# Deprecated and old versions
deprecated/
archived/
old_versions/

# Node modules (if any JS tooling)
node_modules/
package-lock.json

# AWS credentials
.aws/
credentials

# Jupyter notebooks
.ipynb_checkpoints/
*-checkpoint.ipynb

# Claude Code cache
.claude/
.claude.json
.claude.json.backup

# N8N workflows (if contains sensitive data)
.n8n/

# Service PIDs
*.pid

# Python cache
*.pyc
.pytest_cache/

# API keys and secrets (double-check these are excluded!)
*_api_key*
*_secret*
*_token*
secrets/

# State files (runtime state that changes frequently)
.sec_filing_monitor_state.json
.sec_monitor_health.json
.data_quality_approvals.json
.recurring_pattern_alerts.json
.validation_issue_queue.json

# Streamlit config
.streamlit/

# Archive directories
archive/
archives/

# Deprecated and broken files
*.DEPRECATED
*.broken
*.backup.*

# SQL files and database dumps
*.sql
*.sql.gz
backups/*.sql.gz

# HTML test/debug files (exclude templates if you have any)
*.html
!templates/*.html

# Shell scripts (system-specific, keep important setup scripts)
*.sh
!setup*.sh
!install*.sh
!deploy*.sh

# Systemd service files (system-specific)
*.service

# Generated JSON data files
*_enriched.json
spac_research_results.json

# Log directory contents (already ignoring *.log but be explicit)
logs/*.jsonl
logs/*.json
logs/*.gz
logs/rollback_*.sql

# YAML config files (keep templates)
config.yaml
!config.yaml.template
config/agents/*.yaml
!config/agents/*.yaml.template

# Markdown documentation (uncomment to track all docs)
# Comment out the lines below if you want to track documentation
*.md
!README.md
!CLAUDE.md
!TODO.md
!CODEBASE_CLEANUP_RECOMMENDATIONS.md
!DATA_VALIDATOR_COMPARISON.md
!IPO_DETECTION_FAILURE_ROOT_CAUSE.md
!docs/OPPORTUNITY_AGENT_DESIGN.md
!docs/OPPORTUNITY_AGENT_DATA_AUDIT.md

# Prompts directory (keep the structure, ignore specific prompts if sensitive)
# prompts/**/*.yaml
# !prompts/**/*.yaml.template
